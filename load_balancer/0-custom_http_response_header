#!/usr/bin/env bash
# Configures a new Ubuntu server to meet the project requirements
#  - Installs Nginx
#  - Adds X-Served-By header
#  - Adds custom index, redirect, and 404 page

set -e

# Update and install Nginx
apt-get update -y
apt-get install -y nginx

# Hostname for the header
HOSTNAME=$(hostname)

# Set up the web pages
echo "Holberton School" > /var/www/html/index.html
echo "Ceci n'est pas une page" > /var/www/html/404.html

# Backup default Nginx config if not already
CONFIG="/etc/nginx/sites-available/default"
if [ ! -f "${CONFIG}.bak" ]; then
    cp $CONFIG "${CONFIG}.bak"
fi

# Add redirect for /redirect_me if missing
if ! grep -q "rewrite ^/redirect_me" $CONFIG; then
    sed -i "/server_name _;/a \    rewrite ^/redirect_me https://www.youtube.com/watch?v=QH2-TGUlwu4 permanent;" $CONFIG
fi

# Add custom 404 page if missing
if ! grep -q "location = /404.html" $CONFIG; then
    sed -i "/listen 80 default_server;/a \    error_page 404 /404.html;\n    location = /404.html {\n        root /var/www/html;\n        internal;\n    }" $CONFIG
fi

# Add X-Served-By header if missing
if ! grep -q "add_header X-Served-By" $CONFIG; then
    sed -i "/server_name _;/a \    add_header X-Served-By $HOSTNAME;" $CONFIG
fi

# Test Nginx configuration
nginx -t

# Restart Nginx
systemctl restart nginx
